{
  "name": "source_code_generation",
  "purpose": "Generate application source code from module specifications, following the layer structure and naming conventions in architecture.json",
  "inputs": {
    "architecture": {
      "from": "config.mde.architecture",
      "default": "mde/architecture/architecture.json"
    },
    "moduleSchema": {
      "from": "config.design.modules",
      "default": "design/modules/{moduleType}/{module}/schema.json"
    },
    "moduleRules": {
      "from": "config.design.modules",
      "default": "design/modules/{moduleType}/{module}/rules.json"
    },
    "stateMachine": {
      "from": "config.design.modules",
      "default": "design/modules/{moduleType}/{module}/state-machine.json"
    },
    "events": {
      "from": "config.design.modules",
      "default": "design/modules/{moduleType}/{module}/events.json"
    },
    "api": {
      "from": "config.design.modules",
      "default": "design/modules/{moduleType}/{module}/api.yaml"
    }
  },
  "outputs": {
    "src": {
      "to": "config.output.src",
      "pattern": "{module}/**/*.ts"
    }
  },
  "traceability": {
    "method_doc_required": true,
    "applies_to_layers": [
      "controller",
      "service",
      "domain",
      "query_service",
      "data_access"
    ],
    "exclude_artifacts": [
      "dto",
      "repository.interface",
      "mapper"
    ],
    "required_tags": [
      "@requirement",
      "@design_concern"
    ],
    "optional_tags": [
      "@use_case",
      "@business_rule",
      "@module"
    ],
    "format": [
      "/**",
      " * <short behavior summary>",
      " * @requirement FR-xxx, BR-xxx",
      " * @design_concern layer:<layer>, concern:<concern>, ownership:<owner>",
      " * @use_case UC-xxx",
      " */"
    ],
    "rules": [
      "Every generated method in applicable layers must include a TSDoc block immediately above the method declaration.",
      "Use only requirement IDs that exist in BA artifacts (FR-*, BR-*, NFR-*).",
      "If a method is technical-only and has no direct business requirement, use @requirement INTERNAL and still include @design_concern.",
      "Design concerns must map to architecture responsibility categories (for example: validation, authorization, workflow, persistence, concurrency, integration, audit, notification, read_model_shaping)."
    ]
  },
  "scaffold": {
    "package_json": {
      "when": "file does not already exist",
      "derive_from": [
        "config.project.name",
        "config.project.version",
        "mde/architecture/architecture.json tech block"
      ],
      "must_include": {
        "scripts": {
          "build": "tsc",
          "start": "node dist/index.js",
          "test": "jest",
          "test:unit": "jest --testPathPattern=test/unit",
          "test:integration": "jest --testPathPattern=test/integration"
        },
        "dependencies": "derive from architecture.json tech (e.g. mysql2 for MySQL, uuid)",
        "devDependencies": "typescript, ts-jest, @types/jest, jest, @types/node"
      }
    },
    "tsconfig_json": {
      "when": "file does not already exist",
      "derive_from": [
        "mde/architecture/architecture.json tech block"
      ],
      "must_include": {
        "compilerOptions": {
          "target": "ES2020",
          "module": "commonjs",
          "strict": true,
          "esModuleInterop": true,
          "skipLibCheck": true,
          "outDir": "dist",
          "rootDir": "src"
        },
        "include": [
          "src/**/*.ts"
        ],
        "exclude": [
          "node_modules",
          "dist",
          "test"
        ]
      }
    }
  },
  "test": {
    "framework": "jest",
    "root": {
      "from": "config.test.root",
      "default": "test"
    },
    "unit": {
      "from": "config.test.unit",
      "default": "test/unit"
    },
    "integration": {
      "from": "config.test.integration",
      "default": "test/integration"
    },
    "integration_controller_coverage": {
      "note": "Every controller integration test file must cover the full CRUD surface of its module against the real database.",
      "required_test_cases_per_module": [
        "GET list â€” returns 200 with array; returns empty array when no data",
        "GET by ID â€” returns 200 with correct record; returns 404 when ID does not exist",
        "POST create â€” returns 201 with created record; returns 400 when required fields missing; returns 400 when business rule violated (e.g. duplicate code)",
        "PUT update â€” returns 200 with updated record; returns 404 when ID does not exist; returns 400 when fields invalid",
        "PATCH partial update (if applicable) â€” returns 200; returns 404",
        "DELETE or deactivate â€” returns 200; returns 404 when ID does not exist",
        "State transitions (workflow modules only) â€” one test per valid transition (e.g. approve, reject, cancel); one test per invalid transition (e.g. approve an already-approved request returns 409 or 400)",
        "Authorization â€” returns 403 when caller role is not permitted (if role enforcement is implemented)"
      ],
      "setup_pattern": "beforeAll: run db:init equivalent (truncate + seed tables used by module); afterAll: clean up inserted test records",
      "assertion_pattern": "Assert HTTP status code AND response body shape â€” never status code alone",
      "data_pattern": "Insert test fixtures directly via SQL in beforeEach for isolation â€” do not rely on seed data order"
    }
  },
  "artifacts": [
    {
      "from": "config.output.src",
      "default": "src"
    }
  ],
  "generation_order": [
    "shared types and errors",
    "domain Entity",
    "Commands",
    "Repository port interfaces",
    "Service classes",
    "Mapper",
    "Repository implementations",
    "QueryService classes",
    "Controllers",
    "DI container wiring",
    "Unit tests (domain + service) â†’ test/unit/{module}/",
    "Integration tests (data_access + controller) â†’ test/integration/{module}/"
  ],
  "rules": [
    "Read architecture.json for folder structure, naming conventions, and layer rules before generating",
    "Follow codegen.structure.layers for output paths â€” src/{module}/domain/, src/{module}/service/, etc.",
    "Follow codegen.naming for class and file naming conventions",
    "Entity must expose create() factory for new instances and rehydrate() factory for repository reconstruction",
    "Service class: one public method per use case â€” authorize, load, act, save, emit event",
    "QueryService: stateless, reads only, returns ReadDto â€” must not touch domain layer",
    "Mapper: pure field translation only â€” no business logic",
    "Repository implementation: raw SQL, parameterized queries only",
    "Commands: one per write operation, fields only, no behavior",
    "Method-level traceability docs are mandatory for generated methods in controller/service/domain/query_service/data_access layers",
    "Each method doc block must include @requirement and @design_concern tags; add @use_case when applicable",
    "Requirement references in method docs must be valid IDs from BA artifacts (FR-*, BR-*, NFR-*), or INTERNAL for technical-only methods",
    "Event types: typed aliases of DomainEvent<T> â€” base type in shared/events/domain-event.ts",
    "DomainEventPublisher interface in shared/events/domain-event-publisher.ts",
    "Emit deterministic outputs â€” same inputs must produce same outputs",
    "Read config.test.* for test output paths â€” default to test/unit and test/integration",
    "Unit tests go to test/unit/{module}/ â€” cover domain (entity, validator, state-machine) and service layers; mock all dependencies",
    "Integration tests go to test/integration/{module}/ â€” cover data_access (repository against real DB) and controller (HTTP); no mocks",
    "Test framework is Jest â€” use describe/it/expect; import from jest for mocks",
    "Each service test: one describe block per service class, one it() per error condition and one for the happy path",
    "Each entity test: cover create(), rehydrate(), and every state transition method",
    "Each validator test: cover every rule â€” one it() per pass case and one per throw case",
    "Do not generate test files for dto, mapper, or repository interface layers",
    "Generate package.json if it does not exist â€” derive name and version from config.project, tech stack from architecture.json; always include jest scripts for unit and integration",
    "Generate tsconfig.json if it does not exist â€” derive target and module from architecture.json tech; outDir=dist, rootDir=src, exclude test/",
    "Never overwrite package.json or tsconfig.json if they already exist",
    "CRITICAL: The generated src/index.ts (Express entry point) must include ALL write endpoints (POST, PUT, PATCH, DELETE) for every module, not just GET endpoints. For every module that has create/update/delete operations, generate the corresponding mutation routes. Missing mutation endpoints cause silent 404 failures in the UI.",
    "CRITICAL: Every mutation endpoint response must include a descriptive error message in the JSON body under the 'error' key â€” e.g. { error: 'Leave request not found or cannot be modified' }. Generic 500 responses without detail make debugging impossible.",
    "CRITICAL: The dev script must be 'ts-node src/index.ts'. The start script must be 'node dist/index.js'. Never swap them. Running start before build causes 404s from a stale dist/.",
    "The package.json must include a 'dev' script using ts-node for development without a build step."
  ],
  "uses_tools": [
    "file_manager"
  ],
  "next_phase": "development"
}
