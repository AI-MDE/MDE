{
  "name": "ui_source_code_generation",
  "purpose": "Generate frontend UI source code from UI module specs. Produces one component per page, a router, shared layout with menu, and an auth guard — all derived from ui-catalog.json and ui-{module}.json specs.",
  "inputs": {
    "configuration": {
      "from": "config._self",
      "default": "configuration.json"
    },
    "application": {
      "from": "config.application.definition",
      "default": "application/application.json"
    },
    "uiCatalog": {
      "from": "config.design.uiModules",
      "pattern": "ui-catalog.json",
      "default": "design/modules/ui/ui-catalog.json"
    },
    "uiModuleSpecs": {
      "from": "config.design.uiModules",
      "pattern": "ui-*.json",
      "default": "design/modules/ui/ui-*.json"
    },
    "moduleCatalog": {
      "from": "config.design.moduleCatalog",
      "default": "design/modules/module-catalog.json"
    },
    "entities": {
      "from": "config.design.entityPattern",
      "default": "design/entities/ent-*.json"
    },
    "architecture": {
      "from": "config.mde.architecture",
      "pattern": "architecture.json",
      "default": "mde/architecture/architecture.json"
    }
  },
  "outputs": {
    "uiSrc": {
      "to": "config.output.uiSrc",
      "default": "ui/src"
    }
  },
  "tech": {
    "from": "config.ui",
    "fields": {
      "framework":   "react | vue | angular — default react",
      "language":    "typescript — always",
      "router":      "react-router-dom v6 | vue-router | angular-router",
      "styling":     "tailwind | css-modules | styled-components — default tailwind",
      "stateClient": "tanstack-query | swr | none — default tanstack-query",
      "authScheme":  "jwt-bearer | session | none — default jwt-bearer"
    }
  },
  "scaffold": {
    "package_json": {
      "when": "file does not already exist",
      "derive_from": ["config.project.name", "config.project.version", "config.ui"],
      "must_include": {
        "scripts": {
          "dev":   "vite",
          "build": "tsc && vite build",
          "preview": "vite preview",
          "test":  "vitest"
        },
        "dependencies": "derive from config.ui (react/vue + router + tanstack-query + axios)",
        "devDependencies": "typescript, vite, vitest, @types/react or @types/vue"
      }
    },
    "tsconfig_json": {
      "when": "file does not already exist",
      "must_include": {
        "compilerOptions": {
          "target": "ES2020",
          "module": "ESNext",
          "moduleResolution": "bundler",
          "strict": true,
          "jsx": "react-jsx",
          "baseUrl": ".",
          "paths": { "@/*": ["src/*"] }
        }
      }
    },
    "vite_config": {
      "when": "file does not already exist",
      "filename": "vite.config.ts",
      "must_include": {
        "resolve.alias": "@ → src/",
        "server.proxy": "derive from config.ui.apiProxy if set"
      }
    }
  },
  "folder_structure": {
    "note": "All paths are RELATIVE TO config.output.uiSrc (e.g. /dev/myapp/ui/src). Do NOT write to design/ or any other folder.",
    "root": "{config.output.uiSrc}",
    "layout": "{config.output.uiSrc}/layout/ — AppShell, Sidebar, TopBar, AuthGuard",
    "router": "{config.output.uiSrc}/router/ — index.tsx with all routes",
    "types":  "{config.output.uiSrc}/types/ — one .ts file per entity",
    "api":    "{config.output.uiSrc}/api/ — one .ts file per backend module",
    "modules": "{config.output.uiSrc}/modules/{kebab-module-name}/",
    "pages":  "{config.output.uiSrc}/modules/{kebab-module-name}/pages/ — one .tsx per page",
    "components": "{config.output.uiSrc}/modules/{kebab-module-name}/components/"
  },
  "generation_order": [
    "scaffold files in parent of config.output.uiSrc: package.json, tsconfig.json, vite.config.ts if absent",
    "{config.output.uiSrc}/types/{Entity}.ts — one per entity file",
    "{config.output.uiSrc}/api/{kebab-module}.ts — one per backend module",
    "{config.output.uiSrc}/layout/AppShell.tsx",
    "{config.output.uiSrc}/layout/Sidebar.tsx — nav menu from ui-catalog.menu",
    "{config.output.uiSrc}/layout/AuthGuard.tsx",
    "{config.output.uiSrc}/router/index.tsx — all routes, lazy-loaded",
    "{config.output.uiSrc}/modules/{module}/pages/{PageName}.tsx — one per page spec",
    "{config.output.uiSrc}/modules/{module}/index.ts — barrel export"
  ],
  "page_generation_rules": {
    "dashboard": "Render summary cards derived from shows[]. Each card navigates to the target listed in actions[type=navigate].",
    "list": "Render a table or card list. Columns from shows[]. Add filter controls from filters[]. Each row navigates to detail via actions[type=navigate]. Render emptyState when data is empty.",
    "detail": "Render all fields as read-only. Add action buttons from actions[]. Confirm modal before any type=confirm action.",
    "form": "Render a form with one input per field in validation[]. Apply validation rules. Submit via actions[type=submit]. Cancel navigates back.",
    "wizard": "Render a step indicator. Each step is a separate component. Next/Back buttons. Submit only on final step.",
    "modal": "Render as a dialog overlay. Confirm/Cancel buttons only. No navigation within the modal.",
    "split-view": "Left pane = list, right pane = detail or form. Selection in list updates right pane."
  },
  "api_client_rules": [
    "One file per backend module — src/api/{kebab-module}.ts",
    "Derive endpoints from module-catalog exposes[] array",
    "All calls use a shared baseClient (axios or fetch wrapper) from src/api/client.ts",
    "Return typed responses using entity types from src/types/",
    "Auth header injected by baseClient interceptor using authScheme from config.ui"
  ],
  "auth_rules": [
    "AuthGuard wraps every route — redirects to login if unauthenticated",
    "requiredRoles on a page maps to actor names from application.json",
    "If the current user's role is not in requiredRoles, render a 403 component, not a redirect"
  ],
  "naming_rules": {
    "component_file": "PascalCase.tsx — e.g. MyLeaveDashboard.tsx, SubmitLeaveRequest.tsx",
    "route_file":     "kebab-case from routePrefix — e.g. my-leave, team-approvals",
    "api_file":       "kebab-module.ts — matches module-catalog moduleName kebab",
    "type_file":      "PascalCase.ts — matches entity name from ent-*.json"
  },
  "rules": [
    "CRITICAL: config.design.uiModules is the INPUT folder (read-only spec files). config.output.uiSrc is the OUTPUT folder (write all generated code here). Never write into config.design.uiModules.",
    "CRITICAL: Generate .tsx / .ts files only — never .ejs, .hbs, .html, .vue, or any other template format unless config.ui.framework explicitly says so.",
    "Read config.ui for framework and styling choices before generating any file",
    "Read ui-catalog.json menu array to build the router and Sidebar — menu order = route order",
    "Read each ui-{module}.json for page specs — one component per page",
    "subNav in a module spec → render a TabBar or secondary nav within the module's AppShell slot",
    "navigation.menuVisible=true pages are direct routes; menuVisible=false pages are nested/child routes",
    "Every page component receives typed props — no any",
    "Derive column and field names from shows[] and validation[] in the page spec — do not invent fields",
    "Every form must implement all validation rules from the page spec",
    "All list pages must render emptyState string when the data array is empty",
    "All delete/destructive actions must show a confirm dialog before proceeding",
    "Do not generate mock data or hardcoded values — all data comes from api/ functions",
    "Do not overwrite scaffold files (package.json, tsconfig.json, vite.config.ts) if they already exist",
    "Never write outside config.output.uiSrc"
  ],
  "uses_tools": [
    "file_manager"
  ],
  "next_phase": "development"
}
