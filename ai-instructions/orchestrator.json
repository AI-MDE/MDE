{
  "name": "mdt_orchestrator",
  "version": "1.1",
  "description": "Command-driven orchestrator for this workspace's AI-assisted model-driven engineering workflow.",
  "state_model": {
    "project_state_file": { "from": "config.project_state.state", "default": "project/project-state.json" },
    "fields": {
      "current_phase": "string",
      "completed_commands": [
        "string"
      ],
      "failed_commands": [
        "string"
      ],
      "artifacts": {
        "type": "object",
        "additionalProperties": {
          "type": "string",
          "enum": [
            "missing",
            "in_progress",
            "complete",
            "failed"
          ]
        }
      },
      "last_command": "string",
      "last_run_at": "datetime",
      "recommended_next_command": "string",
      "next_valid_commands": [
        {
          "command": "string",
          "reason": "string"
        }
      ]
    }
  },
  "execution_mode": "one_command_per_turn",

  "interaction_policy": {
    "execution_mode": "one_command_per_turn",
    "rules": [
      "Execute exactly one command per user turn â€” never chain commands automatically.",
      "After a command completes, stop. Report results and next_valid_commands, then wait for the user.",
      "Ambiguous input like 'continue' must be resolved by showing next_valid_commands and asking the user to pick one â€” never infer and auto-execute.",
      "Do not infer intent to run multiple commands from vague instructions. Ask instead.",
      "The user must explicitly invoke each next command."
    ]
  },

  "command_resolution": {
    "mode": "explicit_first",
    "rules": [
      "If the user input matches a command label exactly, use that command.",
      "If the user input matches a canonical command name, use that command.",
      "If the user input is close to multiple commands, return ranked candidates.",
      "Never guess destructive or phase-skipping commands.",
      "Normalize user text to verb + target where possible.",
      "If user input cannot be resolved to a command, do not guess â€” show next_valid_commands and ask the user to pick."
    ],
    "examples": [
      {
        "user_input": "Perform Business Analysis",
        "resolved_command": "perform_business_analysis"
      },
      {
        "user_input": "Validate Requirements",
        "resolved_command": "validate_requirements"
      },
      {
        "user_input": "Build System Design",
        "resolved_command": "build_system_design"
      },
      {
        "user_input": "Generate Modules",
        "resolved_command": "generate_modules"
      },
      {
        "user_input": "Generate UI Outline",
        "resolved_command": "generate_ui_outline"
      },
      {
        "user_input": "Generate UI Source Code",
        "resolved_command": "generate_ui_source_code"
      }
    ]
  },
  "execution_pipeline": [
    {
      "step": 1,
      "name": "resolve_command",
      "description": "Map user input to a canonical command in mde/ai-instructions/commands/*.json"
    },
    {
      "step": 2,
      "name": "load_context",
      "description": "Load AGENT.md, orchestration registries, BA artifacts, and optional project state/configuration files"
    },
    {
      "step": 3,
      "name": "check_prerequisites",
      "description": "Verify that all required artifacts for the command exist"
    },
    {
      "step": 4,
      "name": "validate_phase_rules",
      "description": "Ensure the command is valid for the current phase or explicitly allowed cross-phase"
    },
    {
      "step": 5,
      "name": "select_skills_and_tools",
      "description": "Read the command definition and determine which skills and tools to invoke"
    },
    {
      "step": 6,
      "name": "execute_skill_flow",
      "description": "Invoke skill flows in order and let each skill call its permitted tools"
    },
    {
      "step": 7,
      "name": "persist_outputs",
      "description": "Write generated or validated artifacts to the configured BA and project paths. MUST complete before steps 8 and 9."
    },
    {
      "step": 8,
      "name": "update_project_state",
      "description": "MANDATORY: Update project/project-state.json â€” set current_phase, last_command, last_run_at, recommended_next_command, next_valid_commands (with reasons), and artifact statuses. Create the file if it does not exist. Never skip this step."
    },
    {
      "step": 9,
      "name": "append_command_log",
      "description": "MANDATORY: Append one entry to project/logs/command-log.json per logging_policy. Create the file with { \"entries\": [] } if it does not exist. Never skip this step, even on failure or partial output."
    },
    {
      "step": 10,
      "name": "return_result",
      "description": "Return the response_contract payload. Do not return until steps 8 and 9 are complete."
    }
  ],
  "phase_rules": {
    "project_initiation": {
      "allowed_commands": [
        "select_methodology",
        "select_architecture"
      ],
      "exit_conditions_all": [
        "{ config.project_state.state } exists or initialization was intentionally skipped"
      ]
    },
    "business_analysis": {
      "allowed_commands": [
        "identify_domain",
        "identify_external_references",
        "perform_business_analysis",
        "generate_business_functions",
        "generate_use_cases",
        "validate_requirements"
      ],
      "exit_conditions_all": [
        "{ config.ba.requirements } exists",
        "{ config.ba.analysisStatus } exists",
        "{ config.project_state.questions } exists",
        "{ config.project_state.openQueue } exists"
      ]
    },
    "system_design": {
      "allowed_commands": [
        "build_system_design",
        "generate_ldm",
        "validate_ldm_coverage",
        "generate_modules",
        "elaborate_system_design",
        "generate_ui_outline",
        "validate_architecture"
      ],
      "exit_conditions_all": [
        "{ config.design.appArchitecture } exists",
        "{ config.design.moduleCatalog } exists",
        "at least one module definition exists in { config.design.modules }"
      ]
    },
    "development": {
      "allowed_commands": [
        "generate_source_code",
        "generate_ui_source_code",
        "generate_sample_data"
      ],
      "exit_conditions_all": [
        "development artifacts exist for the target command"
      ]
    },
    "governance": {
      "allowed_commands": [
        "assess_methodology",
        "assess_architecture",
        "validate_mde_framework",
        "validate_project_architecture_compliance",
        "validate_design_compliance",
        "validate_project_structure",
        "evaluate_change_request",
        "validate_traceability",
        "show_phase_status",
        "generate_diagrams",
        "generate_documentation",
        "export_docs"
      ],
      "exit_conditions_all": []
    }
  },
  "skill_invocation_policy": {
    "rules": [
      "A command may call one or more skills in sequence.",
      "A skill may only use tools declared in mde/ai-instructions/skills/*.json and allowed by the command.",
      "Skills must not invent missing upstream artifacts.",
      "Validation skills may report missing artifacts but must not create them unless explicitly allowed."
    ]
  },
  "tool_invocation_policy": {
    "rules": [
      "file_manager is the default tool for artifact I/O.",
      "json_validator must be used when a command produces or validates JSON artifacts.",
      "traceability_engine must be used for end-to-end trace checks.",
      "architecture_validator must be used for architecture conformance checks.",
      "No tool may write outside configured project paths.",
      "The MDE framework folder (config.mde.path) is READ-ONLY. Skills and commands MUST NOT write any files into it. All output goes to project paths defined in configuration.json under ba, design, project_state, application, output, or test."
    ]
  },
  "error_handling": {
    "on_missing_prerequisites": {
      "action": "block_execution",
      "response": "Return missing artifacts and recommended preceding commands."
    },
    "on_validation_failure": {
      "action": "complete_with_warnings",
      "response": "Return validation report and keep command status as failed or warning depending on severity."
    },
    "on_tool_failure": {
      "action": "stop_current_command",
      "response": "Log tool failure, mark command failed, preserve partial outputs only if explicitly allowed."
    },
    "on_phase_mismatch": {
      "action": "reject_or_offer_override",
      "response": "Explain phase mismatch and show valid commands for the current state."
    }
  },
  "response_contract": {
    "fields": [
      "command",
      "status",
      "summary",
      "outputs",
      "warnings",
      "blockers",
      "next_valid_commands"
    ],
    "status_values": [
      "success",
      "warning",
      "blocked",
      "failed"
    ]
  },
  "logging_policy": {
    "log_file": { "from": "config.project_state.commandLog", "default": "project/logs/command-log.jsonl" },
    "rules": [
      "After completing or failing any AI command, append one entry to { config.project_state.commandLog }.",
      "If the file does not exist, create it as an empty file.",
      "Append one newline-delimited JSON line per command â€” never overwrite existing lines.",
      "Entry schema: { command, label, ai: true, ran_at (ISO 8601), status (success|warning|failed), cost_estimate (object with input_tokens and output_tokens if known, otherwise null), outputs (array of files written) }",
      "cost_estimate should be filled when token counts are available from the session context; otherwise set to null.",
      "This rule is mandatory â€” do not skip logging even if the command produced warnings or partial output."
    ]
  },
  "post_command_obligations": {
    "description": "These actions are REQUIRED at the end of every command execution, success or failure.",
    "obligations": [
      {
        "action": "update_project_state",
        "file": { "from": "config.project_state.state", "default": "project/project-state.json" },
        "fields_required": ["current_phase", "last_command", "last_run_at", "recommended_next_command", "next_valid_commands", "artifacts"]
      },
      {
        "action": "append_command_log",
        "file": { "from": "config.project_state.commandLog", "default": "project/logs/command-log.jsonl" },
        "fields_required": ["command", "label", "ai", "ran_at", "status", "outputs"]
      }
    ],
    "enforcement": "A command is not considered complete until both obligations are fulfilled."
  },

  "next_command_policy": {
    "rules": [
      "Recommend only commands whose prerequisites are satisfied.",
      "Prefer commands in the current phase before cross-phase commands.",
      "Always include at least one validation or inspection command when available."
    ]
  }
}
