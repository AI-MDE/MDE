1 generate code for all tools required by commands
    with testing and docs
2 Separate Modules by Type, Naming by Purpose (srv-01,dal-03)
3 Explain here what is the onboarding-orchestrator
4 Allow UI to view Methodology , Architecture and App-Architecture
5 We need Sample\output folder to hold logs and validation-reports
6 sample\configuration.json 
    has two columns "standard-folder-names" "physical-folder-names"
    therefore, MDT Folder must not user physical-folder-name only standard-folder-name
    need those "standard-names" registered somewhere? so we can validate configuration.json

eleminate duplication between commands.json and commands.md


I am assuming all generation will respect changes I make to source code, specs

- Validate requirements will accepts various .md files and convert them into .json
Once we go Live how the structure going to be:

-git clone MDE repo
-cd MDE
- Using AI or enter the command:
- To start clean project
- MD newProj
- CD newProj
- MDE initiate project

-git clone Sample Project


